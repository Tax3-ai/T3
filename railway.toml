[build]
builder = "NIXPACKS"
buildCommand = "npx prisma generate && npx prisma db push && npx next build"

[deploy]
startCommand = "npx next start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
